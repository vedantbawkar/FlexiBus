import 'package:flutter/material.dart';

class RideProfileScreen extends StatelessWidget {
  const RideProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(child: Text("Profile Screen"),);
  }
}