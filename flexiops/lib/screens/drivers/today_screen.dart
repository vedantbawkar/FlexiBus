import 'package:flutter/material.dart';

class RideTodayScreen extends StatelessWidget {
  const RideTodayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(child: Text("My Rides"),);
  }
}